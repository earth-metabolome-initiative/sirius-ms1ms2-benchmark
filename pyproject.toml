[project]
name = "sirius-ms2-vs-ms12"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Marco Visani", email = "marco.visani@unifr.ch" }]
requires-python = ">=3.13"
dependencies = [
    "dictances>=1.5.3",
    "environments-utils>=1.0.15",
    "metabolite-annotator",
    "pandas>=2.3.3",
    "tqdm>=4.67.3",
]

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
metabolite-annotator = { git = "https://github.com/earth-metabolome-initiative/metabolite-annotator" }

[dependency-groups]
dev = [
    "jupyterlab>=4.5.6",
]
